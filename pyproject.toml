[project]
name = "Call_me_maybe"
version = "0.1.0"
description = "LLM function calling project"
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "pydantic",
]

[dependency-groups]
dev = [
    "flake8",
    "mypy",
    "pytest",
]