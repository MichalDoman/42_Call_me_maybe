[project]
name = "Call_me_maybe"
version = "0.1.0"
description = "LLM function calling project"
requires-python = ">=3.10"
dependencies = [
    "huggingface-hub>=1.26.0",
    "numpy",
    "pydantic",
    "torch>=2.13.0",
    "transformers>=5.14.1",
]

[dependency-groups]
dev = [
    "flake8",
    "mypy",
    "pytest",
]
